groups:
  - name: jetson_alerts
    rules:
      - alert: HighGPUUsage
        expr: jetson_gpu_usage > 90
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "Jetson GPU usage high"
          description: "GPU usage is above 90% for over 1 minute."
